package com.mivio.wallet.core.designsystem.theme

import androidx.compose.ui.graphics.Color

// Màu chủ đạo (Brand Colors)
val Primary = Color(0xFF00BFA5)       // Xanh Mint đậm (Tươi mới, tin cậy)
val PrimaryVariant = Color(0xFF008E76)
val Secondary = Color(0xFFB2DFDB)     // Xanh Mint nhạt (Dùng cho Header, nền phụ)

// Màu Nền & Thẻ (Background & Surface)
val Background = Color(0xFFF8F9FA)    // Trắng xám (Nền app)
val Surface = Color(0xFFFFFFFF)       // Trắng tinh (Nền Card, To-do list)
val SurfaceVariant = Color(0xFFEEEEEE)// Xám nhạt (Đường kẻ divider, nền icon)

// Màu Chữ (Typography Colors)
val TextPrimary = Color(0xFF1D1B20)   // Đen nhạt (Chữ chính, Tiêu đề)
val TextSecondary = Color(0xFF49454F) // Xám (Chữ phụ, thời gian, mô tả)
val TextHint = Color(0xFF9E9E9E)      // Xám nhạt (Placeholder trong ô nhập)

// Màu Trạng thái Tài chính (Status Colors)
val IncomeColor = Color(0xFF2E7D32)   // Xanh lá đậm (Tiền cộng vào)
val ExpenseColor = Color(0xFFD32F2F)  // Đỏ (Tiền trừ đi)
val WarningColor = Color(0xFFF57C00)  // Cam (Cảnh báo vượt ngân sách)

// Màu Nền Danh mục (Category Backgrounds)
val CatFood = Color(0xFFFFE0B2)       // Cam nhạt
val CatHouse = Color(0xFFB2EBF2)      // Xanh dương nhạt
val CatTransport = Color(0xFFE1BEE7)  // Tím nhạt